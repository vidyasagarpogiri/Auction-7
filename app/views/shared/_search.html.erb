<form action="<%= items_path %>" method="get">
<div class="input-append">
  <%= text_field_tag :search, params[:search], placeholder: "Search items..." %><br>
  <%= select_tag(:category, options_for_select(@categories, params[:category]), prompt: "Select catgegory") %>
  <%= submit_tag "Search", name: nil %>
</div>
</form>